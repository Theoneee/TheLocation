apply plugin: 'com.android.library'
apply from: "../base.gradle"

android {

    defaultConfig {
        versionCode 5
        versionName "2.0.0"
    }

    sourceSets {
        main() {
            jniLibs.srcDirs = ['libs']
        }
    }

}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    api files('libs/AMap_Location_V4.7.0_20190708.jar')
}
