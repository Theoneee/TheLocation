apply plugin: 'com.android.library'
apply from: "../base.gradle"

android {

    defaultConfig {
        versionCode 5
        versionName "2.0.0"
    }

    sourceSets {
        main() {
            jniLibs.srcDirs = ['libs']
        }
    }

}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    api files('libs/AMap_Location_V6.2.0_20221026.jar')
}
